{
  "schema_version": 1,
  "adapter_kind": "test-memory",
  "driver": {
    "transport": "cli",
    "command": {
      "executable": "test/fixtures/tracker_conformance/fake_task_source_driver.sh",
      "args": ["--mode", "serve"],
      "cwd": ".",
      "env": [{ "name": "TEST_MODE", "value": "fixture" }]
    },
    "timeout_ms": 5000
  },
  "profile": {
    "name": "task_source",
    "capabilities": ["task_source"],
    "adapter_operations": [
      "task_source.fetch_candidates",
      "task_source.refresh_by_refs",
      "task_source.lookup_by_operator_ref"
    ]
  },
  "fixtures": {
    "task_file": "test/fixtures/tracker_conformance/task-source-fetch.response.json",
    "tasks": [
      {
        "name": "primary-card",
        "ref": {
          "backend_kind": "test-memory",
          "remote_id": "card-1",
          "key": "CARD-1",
          "url": "https://tracker.example/tasks/CARD-1"
        },
        "operator_refs": ["CARD-1"],
        "purpose": "Stable identity fixture for refresh and lookup cases"
      }
    ]
  },
  "report": {
    "redact": ["SECRET_TOKEN"]
  }
}
