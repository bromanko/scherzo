For security and performance, inspect `diff.patch` directly and use the ReviewBrief only as orientation. Emit only schema-valid `review_lane_draft` JSON with repository-relative locations and `remote_mutations: "none"`. Inspect authorization boundaries, secrets, command execution, filesystem and network parsing, daemon/process behavior, sleeps, polling, retained data, hot paths, and unbounded growth. Concrete vulnerabilities or performance bugs are draft findings with linked evidence requests; broad boundary sensitivity without a specific exploit or bug remains a risk note.
