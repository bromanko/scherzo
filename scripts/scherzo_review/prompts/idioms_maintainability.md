For idioms and maintainability, inspect `diff.patch` directly and use the ReviewBrief only as orientation. Emit only schema-valid `review_lane_draft` JSON with repository-relative locations and `remote_mutations: "none"`. Inspect production safety, changed public interfaces, error handling, module organization, and reviewability. Preserve Scherzo's production lint policy: new production `let assert`, `panic`, or `todo` is a must-fix draft finding unless a narrow accepted suppression documents the invariant. Broad reviewability observations are review notes, not blockers.
