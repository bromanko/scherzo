For correctness, inspect `diff.patch` directly and use the ReviewBrief only as orientation. Emit only schema-valid `review_lane_draft` JSON with repository-relative locations and `remote_mutations: "none"`. Focus on behavior bugs, broken invariants, inverted conditions, parser/control mistakes, state-machine errors, and regressions. A proposed blocking correctness finding must include an evidence request for same-finding executable proof (`test`, `runtime`, or `fixture_reproduction`); static-only concerns must be non-blocking suspicions or review notes with concrete follow-up.
