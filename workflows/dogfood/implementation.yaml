# yaml-language-server: $schema=../../schemas/scherzo.workflow.v1.schema.json
version: 1
id: implementation
description: Implement a focused Scherzo task from Linear-fetched context, review it, validate it, publish a PR, and report the PR URL.
workspace:
  driver: dogfood-jj
  requires: [status, diff, changed-files, baseline, refresh-base, publish-commit-stack]
concurrency: 4
recovery:
  attempts: 1
  prompt: prompts/implementation-recover-failed-step.md
contract:
  version: 1
  outputs:
    commit_stack:
      kind: commit_stack
      media_type: application/vnd.scherzo.git-commit-stack+json
      artifact_type: scherzo.git_commit_stack.v1
      source:
        step: materialize_commit_stack
        path: state/implementation/scherzo-implementation-commit-stack.json
artifacts:
  publications:
    - id: implementation_commit_stack
      repository: github.code
      required: true
      mode: commit_stack
      commit_stack:
        select:
          output: commit_stack
      target:
        kind: stable_branch
steps:
  - id: prepare_context
    kind: command
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" prepare --source ticket'
    timeout: 5m
    run_in: main

  - id: refresh_base_before_implementation
    kind: command
    depends_on: [prepare_context]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" refresh-base --stage before-implementation'
    timeout: 5m
    run_in: main

  - id: implement
    kind: agent
    depends_on: [refresh_base_before_implementation]
    prompt: prompts/implement.md
    run_in: main

  - id: analyze_changes
    kind: command
    depends_on: [implement]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" analyze'
    run_in: main

  - id: validate_before_native_review
    kind: command
    depends_on: [analyze_changes]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" validate'
    timeout: 20m
    on_failure: continue
    run_in: main

  - id: prepare_review
    kind: command
    depends_on: [validate_before_native_review]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; output_dir="$SCHERZO_RUN_ROOT/artifacts/review/prepare_review"; dirty_tree_dir="$SCHERZO_RUN_ROOT/artifacts/review/dirty_tree"; cutover_contract_dir="$SCHERZO_RUN_ROOT/artifacts/review/cutover_contract"; if [ -f "$SCHERZO_RUN_ROOT/input.diff" ]; then "$bundle_dir/scripts/scherzo-review" prepare-native --diff-file "$SCHERZO_RUN_ROOT/input.diff" --output-dir "$output_dir" --source-label "implementation-native" --validation-result state/implementation/scherzo-implementation-validation.json --validation-log-ref .scherzo/command-step-diagnostics/validate_before_native_review.txt --dirty-tree-dir "$dirty_tree_dir" --cutover-contract-dir "$cutover_contract_dir"; else "$bundle_dir/scripts/scherzo-review" prepare-native --from "${SCHERZO_REVIEW_FROM:-@-}" --to "${SCHERZO_REVIEW_TO:-@}" --output-dir "$output_dir" --source-label "implementation-native" --validation-result state/implementation/scherzo-implementation-validation.json --validation-log-ref .scherzo/command-step-diagnostics/validate_before_native_review.txt --dirty-tree-dir "$dirty_tree_dir" --cutover-contract-dir "$cutover_contract_dir"; fi'
    timeout: 5m
    run_in: main


  - id: lane_correctness
    kind: agent
    depends_on: [prepare_review]
    prompt: prompts/review-native-correctness.md
    on_failure: continue
    run_in:
      name: review-correctness
      from: main
    structured_output:
      format: json
      artifact_name: correctness_submission
      source:
        type: pi_tool_call
        tool_name: submit_review_lane_draft
        parameters_schema_path: .scherzo/workflows/schemas/provider/review-lane-draft.correctness.v1.schema.json
      required: true
      validators:
        - name: review_lane_submission_shape
          type: json_schema
          path: .scherzo/workflows/schemas/provider/review-lane-draft.correctness.v1.schema.json
          draft: "2020-12"
      schema:
        type: object
        required:
          - draft_findings
          - review_notes
          - evidence_requests
          - self_check

  - id: lane_test_quality
    kind: agent
    depends_on: [prepare_review]
    prompt: prompts/review-native-test-quality.md
    model: openai-codex/gpt-5.4
    thinking: medium
    on_failure: continue
    run_in:
      name: review-test-quality
      from: main
    structured_output:
      format: json
      artifact_name: test_quality_submission
      source:
        type: pi_tool_call
        tool_name: submit_review_lane_draft
        parameters_schema_path: .scherzo/workflows/schemas/provider/review-lane-draft.test-quality.v1.schema.json
      required: true
      validators:
        - name: review_lane_submission_shape
          type: json_schema
          path: .scherzo/workflows/schemas/provider/review-lane-draft.test-quality.v1.schema.json
          draft: "2020-12"
      schema:
        type: object
        required:
          - draft_findings
          - review_notes
          - evidence_requests
          - self_check

  - id: lane_idioms_maintainability
    kind: agent
    depends_on: [prepare_review]
    prompt: prompts/review-native-idioms-maintainability.md
    model: openai-codex/gpt-5.4
    thinking: medium
    on_failure: continue
    run_in:
      name: review-idioms-maintainability
      from: main
    structured_output:
      format: json
      artifact_name: idioms_maintainability_submission
      source:
        type: pi_tool_call
        tool_name: submit_review_lane_draft
        parameters_schema_path: .scherzo/workflows/schemas/provider/review-lane-draft.idioms-maintainability.v1.schema.json
      required: true
      validators:
        - name: review_lane_submission_shape
          type: json_schema
          path: .scherzo/workflows/schemas/provider/review-lane-draft.idioms-maintainability.v1.schema.json
          draft: "2020-12"
      schema:
        type: object
        required:
          - draft_findings
          - review_notes
          - evidence_requests
          - self_check

  - id: lane_security_performance
    kind: agent
    depends_on: [prepare_review]
    prompt: prompts/review-native-security-performance.md
    on_failure: continue
    run_in:
      name: review-security-performance
      from: main
    structured_output:
      format: json
      artifact_name: security_performance_submission
      source:
        type: pi_tool_call
        tool_name: submit_review_lane_draft
        parameters_schema_path: .scherzo/workflows/schemas/provider/review-lane-draft.security-performance.v1.schema.json
      required: true
      validators:
        - name: review_lane_submission_shape
          type: json_schema
          path: .scherzo/workflows/schemas/provider/review-lane-draft.security-performance.v1.schema.json
          draft: "2020-12"
      schema:
        type: object
        required:
          - draft_findings
          - review_notes
          - evidence_requests
          - self_check

  - id: finalize_lanes
    kind: command
    depends_on: [lane_correctness, lane_test_quality, lane_idioms_maintainability, lane_security_performance]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; artifact_dir="${SCHERZO_RUN_ARTIFACT_DIR:-$SCHERZO_RUN_ROOT/.scherzo-state/artifacts/runs/$SCHERZO_RUN_ID}"; if [ ! -e "$artifact_dir" ]; then artifact_dir="$(cd "$SCHERZO_RUN_ROOT/../../.." && pwd -P)/.scherzo-state/artifacts/runs/$SCHERZO_RUN_ID"; fi; "$bundle_dir/scripts/scherzo-review" finalize-lanes --prepare-dir "$SCHERZO_RUN_ROOT/artifacts/review/prepare_review" --review-root "$SCHERZO_RUN_ROOT/artifacts/review" --dirty-tree-dir "$SCHERZO_RUN_ROOT/artifacts/review/dirty_tree" --artifact-dir "$artifact_dir" --synthesis-output-dir "$SCHERZO_RUN_ROOT/artifacts/review/synthesize_review" --lane correctness --lane test-quality --lane idioms-maintainability --lane security-performance'
    run_in: main

  # Targeted remediation consumes synthesized native review findings; this is not a second broad review pass.
  - id: apply_feedback
    kind: agent
    depends_on: [finalize_lanes]
    prompt: prompts/apply-feedback.md
    run_in: main
    structured_output:
      format: json
      artifact_name: review_finding_disposition_input
      source:
        type: pi_tool_call
        tool_name: submit_review_finding_dispositions
        parameters_schema_path: .scherzo/workflows/schemas/provider/review-finding-dispositions.v1.schema.json
      required: true
      validators:
        - name: review_finding_disposition_input_semantics
          type: command
          argv:
            - python3
            - .scherzo/workflows/scripts/scherzo-review
            - validate-structured-output
            - --validator
            - review_finding_disposition_input
          working_directory: repository
          timeout: 30s
        - name: review_finding_disposition_provider_shape
          type: json_schema
          path: .scherzo/workflows/schemas/provider/review-finding-dispositions.v1.schema.json
          draft: "2020-12"
        - name: review_finding_disposition_input_schema
          type: json_schema
          path: .scherzo/workflows/schemas/review-finding-disposition-input.v1.schema.json
          draft: "2020-12"
      schema:
        type: object
        required:
          - schema_version
          - artifact_type
          - entries

  - id: materialize_review_dispositions
    kind: command
    depends_on: [apply_feedback]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; artifact_dir="${SCHERZO_RUN_ARTIFACT_DIR:-$SCHERZO_RUN_ROOT/.scherzo-state/artifacts/runs/$SCHERZO_RUN_ID}"; if [ ! -e "$artifact_dir" ]; then artifact_dir="$(cd "$SCHERZO_RUN_ROOT/../../.." && pwd -P)/.scherzo-state/artifacts/runs/$SCHERZO_RUN_ID"; fi; "$bundle_dir/scripts/scherzo-review" materialize-disposition-input --artifact-dir "$artifact_dir" --submission-step apply_feedback --submission-artifact review_finding_disposition_input --output tmp/review-finding-dispositions.v1.json'
    run_in: main

  - id: refresh_and_validate_after_review
    kind: command
    depends_on: [materialize_review_dispositions]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" refresh-base-and-validate --stage before-validation'
    timeout: 20m
    run_in: main
    recovery:
      attempts: 1
      prompt: prompts/repair-base-drift.md

  - id: finalize_review_dispositions
    kind: command
    depends_on: [refresh_and_validate_after_review]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; diff_path=""; if [ -f "$SCHERZO_RUN_ROOT/input.diff" ]; then diff_path=" --diff-file $SCHERZO_RUN_ROOT/input.diff"; fi; "$bundle_dir/scripts/scherzo-review" finalize-dispositions --final-review "$SCHERZO_RUN_ROOT/artifacts/review/synthesize_review/final-review.v1.json" --disposition-input tmp/review-finding-dispositions.v1.json --validation-artifact state/implementation/scherzo-implementation-validation.json --output-dir "$SCHERZO_RUN_ROOT/artifacts/review/final_dispositions" --require-publishable${diff_path}'
    run_in: main

  - id: materialize_commit_stack
    kind: command
    depends_on: [finalize_review_dispositions]
    run: 'bundle_dir=${SCHERZO_WORKFLOW_BUNDLE_DIR:-}; if [ -z "$bundle_dir" ]; then bundle_dir="$(cd "$SCHERZO_CONFIG_DIR/workflows" && pwd -P)"; fi; repo_root=${SCHERZO_REPO_ROOT:-$(cd "$SCHERZO_CONFIG_DIR/.." && pwd -P)}; "$bundle_dir/scripts/scherzo-implementation" publish'
    timeout: 20m
    run_in: main
    recovery:
      enabled: false
