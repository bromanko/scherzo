For test quality, inspect `diff.patch` directly and use the ReviewBrief only as orientation. Emit only schema-valid `review_lane_draft` JSON with repository-relative locations and `remote_mutations: "none"`. Compare implementation changes, changed tests, nearby context, and validation status. Concrete missing or misleading coverage can be a testing draft finding with a linked evidence request; uncertainty is a coverage note with a proposed test. Do not claim that a generic green test run verifies an arbitrary finding.
