Implement the assigned Scherzo UI design iteration.

Use `tmp/scherzo-ui-design-context.md` when it exists. Keep visual artifacts local-only, do not commit screenshots or videos, and preserve the normal Scherzo UI validation path.

If you change `apps/control`, follow `apps/control/ARCHITECTURE.md`.
