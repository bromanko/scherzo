Published by Scherzo's same-repo `commit_stack` publication route.

- Work: `{{ work.identifier }}`
- Workflow: `{{ workflow.id }}`
- Publication: `{{ publication.id }}`
- Version: `{{ publication.version_id }}`

Validation and review evidence are retained in the Scherzo workflow run artifacts and Linear result attachment.
