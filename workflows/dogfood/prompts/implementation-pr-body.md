## Summary

This PR publishes the retained implementation commit stack for `{{ work.identifier }}`.

The implementation workspace was validated, reviewed, and exported as a first-class Scherzo commit-stack artifact before publication.

## Published artifact

{{ publication.files_markdown }}

## Publication details

- Workflow: `{{ workflow.id }}`
- Publication route: `{{ publication.id }}`
- Version: `{{ publication.version_id }}`
- Target repo: `{{ github.repo }}`
- Base branch: `{{ github.base }}`

## Review guidance

Review the commit stack for code, tests, docs, and any intentional plan-file edits included in the implementation.
