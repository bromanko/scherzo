---
id: s-4430
status: open
deps: []
links: []
created: 2026-01-19T23:30:38Z
type: task
priority: 2
assignee: Brian Romanko
tags: [phase3, parsing]
---
# Parse ticket markdown files

Create parsing logic for .tickets/*.md files.

Ticket files have:
- YAML frontmatter (id, title, status, priority, dependencies, etc.)
- Markdown body (description)

Functions needed:
- parse_ticket_file(path) -> Result(Ticket, String)
- parse_frontmatter(content) -> Result(TicketMetadata, String)
- extract_description(content) -> String

Use tom for YAML parsing (or implement simple parser).
Convert to internal Task type for orchestrator use.

